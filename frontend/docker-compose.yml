version: '3.8'

services:
  livetrans:
    image: node:20
    container_name: livetrans
    restart: always
    working_dir: /app
    volumes:
      - ./.output:/app
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    command: node server/index.mjs